* Find out stuff about blk_queue_limit and make_request
** At least it appears in /sys/class/block/<name>/queue/hw_sector_size
* Implement --with-debug
* Use BDEVNAME_SIZE from fs.h
* Find out why 8192 block size locks up the system
* Find out how to user zero copy IO
* Use argparse in bdtun and bdtunloop programs
* The following capabilities should be create parameters
  (from linux/blk_types.h)
        * FLUSH (REQ_FLUSH)
        * FUA (REQ_FUA)
        * DISCARD (REQ_DISCARD)
        * SECURE DISCARD (REQ_DISCARD | REQ_SECURE)

* Create python binding for quick prototyping
