* Create master device
* Add command handling code into the driver
  * THREAD SAFETY!!!
* Compile user space library
  * create
  * list
  * info
  * remove
  * open(returns file handler)
* Add block device request tunneling (structs, .h, lib)
  * THREAD SAFETY!!!
* Create python binding for quick prototyping
